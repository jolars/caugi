# Is it a node expr?

Check if the expression is a valid node expression: symbol, string,
number, c(...), +, (...)

## Usage

``` r
.is_node_expr(expr)
```

## Arguments

- expr:

  An expression to check

## Value

TRUE if the expression is a valid node expression, FALSE otherwise
