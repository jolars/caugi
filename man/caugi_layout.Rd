% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{caugi_layout}
\alias{caugi_layout}
\title{Compute Graph Layout}
\usage{
caugi_layout(
  x,
  method = c("auto", "sugiyama", "fruchterman-reingold", "kamada-kawai")
)
}
\arguments{
\item{x}{A \code{caugi} object. Must contain only directed edges for Sugiyama
layout.}

\item{method}{Character string specifying the layout method. Options:
\itemize{
\item \code{"auto"}: Automatically choose sugiyama for graphs with only directed
edges, otherwise fruchterman-reingold (default)
\item \code{"sugiyama"}: Hierarchical layout for DAGs (requires only directed edges)
\item \code{"fruchterman-reingold"}: Fruchterman-Reingold spring-electrical layout
(fast, works with all edge types).
\item \code{"kamada-kawai"}: Kamada-Kawai stress minimization (high quality, better
distance preservation, works with all edge types).
}}
}
\value{
A \code{data.frame} with columns \code{name}, \code{x}, and \code{y} containing node
names and their coordinates.
}
\description{
Computes node coordinates for graph visualization using
specified layout algorithm. If the graph has not been built yet, it will
be built automatically before computing the layout.
}
\examples{
cg <- caugi(
  A \%-->\% B + C,
  B \%-->\% D,
  C \%-->\% D,
  class = "DAG"
)
layout <- caugi_layout(cg)

}
\seealso{
Other plotting: 
\code{\link{caugi_plot}()},
\code{\link{plot}()}
}
\concept{plotting}
